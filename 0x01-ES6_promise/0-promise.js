export default function getResponseFromAPI() {
  return new Promise(() => {
    // return new Promise((resolve, reject) => {}) is also valid.
    // Retrieve something, like data, by making a request.
    // If it's successful, invoke the resolve function and pass the data along.
    // In case of an error, call the reject function and pass the error.
  });
}
